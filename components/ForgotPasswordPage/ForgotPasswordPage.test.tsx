import { describe, it } from 'vitest';

describe('ForgotPasswordPage', () => {
  it.skip('TODO: covers reset-request submission and confirmation states', () => {
    // ForgotPasswordPage calls passwordResetApi and renders marketing auth chrome with
    // next/navigation. A meaningful smoke test needs to mock the API and router. Add
    // cover when password-reset flow needs direct unit guarding.
  });
});
