import { describe, it } from 'vitest';

describe('RegisterPage', () => {
  it.skip('TODO: covers email/password submit and OAuth chrome', () => {
    // RegisterPage talks to the auth backend (getClientServerUrl, authSession), uses
    // next/navigation, and renders marketing chrome. A meaningful smoke test needs to
    // mock @/lib/env, fetch, and next/navigation. Add cover when registration regresses.
  });
});
