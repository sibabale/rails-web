import { describe, it } from 'vitest';

describe('DashboardOverviewV2', () => {
  it.skip('TODO: covers overview metrics, onboarding state, and DB-setup callouts', () => {
    // DashboardOverviewV2 depends on the redux store, useOnboarding, databaseConnectionsApi,
    // databaseSetupState, and the marketing docs CTA. A meaningful unit smoke needs a
    // store provider, mocked APIs, and the onboarding hook. Keep covered indirectly via
    // dashboard-environment integration tests until direct cover is required.
  });
});
