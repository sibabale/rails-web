import { describe, it } from 'vitest';

describe('InfrastructureSdkCodeBlock', () => {
  it.skip('TODO: renders SDK tab switcher with infrastructure code samples', () => {
    // InfrastructureSdkCodeBlock pulls in marketing SDK snippets and tab state. A
    // meaningful smoke test should assert tab switching renders each language sample;
    // add when the SDK code samples need direct unit cover.
  });
});
