import { describe, it } from 'vitest';

describe('ResetPasswordPage', () => {
  it.skip('TODO: covers token validation and reset submission', () => {
    // ResetPasswordPage calls passwordResetApi and renders marketing auth chrome with
    // next/navigation. A meaningful smoke test needs to mock the API and router. Add
    // cover when password-reset flow needs direct unit guarding.
  });
});
